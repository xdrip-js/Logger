{
  "name": "Logger",
  "version": "1.2.2",
  "description": "Logger xdrip openaps",
  "main": "logger/index.js",
  "license": "MIT",
  "scripts": {
    "test": "make test",
    "setup": "./bin/logger-setup.sh",
    "global-install": "./bin/upgrade-node.sh && npm install && ./bin/logger-setup.sh"
  },
  "dependencies": {
    "xdrip-js": "xdrip-js/xdrip-js#v2.1.2"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/xdrip-js/Logger.git"
  },
  "homepage": "https://github.com/xdrip-js/Logger",
  "config": {
    "blanket": {
      "pattern": [
        "bin"
      ],
      "data-cover-never": [
        "node_modules"
      ]
    }
  }
}
