{
  "name": "Logger",
  "version": "1.2.4",
  "description": "Logger xdrip openaps",
  "main": "logger/index.js",
  "license": "MIT",
  "scripts": {
    "test": "make test",
    "setup": "./bin/logger-setup.sh",
    "global-install": "rm -rf ./node-modules/xdrip-js && ./bin/upgrade-node.sh && npm install && ./bin/logger-setup.sh"
  },
  "dependencies": {
    "xdrip-js": "xdrip-js/xdrip-js#v2.1.8"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/xdrip-js/Logger.git"
  },
  "homepage": "https://github.com/xdrip-js/Logger",
  "config": {
    "blanket": {
      "pattern": [
        "bin"
      ],
      "data-cover-never": [
        "node_modules"
      ]
    }
  }
}
